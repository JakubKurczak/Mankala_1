#include "Player.h"

int Player::get_move(std::shared_ptr<Board> board)
{
	/*while (true) {
		std::cout << this->name << ": ";
		char choice;
		std::cin >> choice;

		if (std::isdigit(choice) && board->is_legal_move(std::atoi(&choice), this)) {
			return std::atoi(&choice);
		}

	}*/

	return 0;
}
